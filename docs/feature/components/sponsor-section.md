# SponsorSection

## 목적

후원사 영역과 후원 문의 CTA 제공.

## 동작/UI

- 상단 설명 영역(배경 gray-50) + 목업 카드 3개.
- 이 섹션은 `SectionTitle`의 `Chip`을 사용하지 않고 제목 텍스트만 노출.
- 모바일/데스크탑 모두 카드 3개가 한 줄에 배치되며, 간격(`gap`)을 제외한 폭을 3등분해 각 카드가 동일 폭을 차지.
- 카드 내부 텍스트는 노출하지 않고, 회색 배경 직사각형 목업만 렌더링.
- 하단 `Button(color=gray)` 기반 문의 CTA.
