# CultureSection

## 목적
딸깍톤 문화 소개 카드 영역 제공.

## 동작/UI
- blue gradient 배경.
- `SectionTitle(Culture)` 사용, 제목은 white.
- 타이틀은 `max-w-[1280px]` 컨테이너 기준으로 중앙 정렬.
- `CultureCardRoller`는 `w-screen` 풀블리드로 노출되어 좌우가 화면 너비를 가득 채움.
